<h1>Pages</h1>
<p>
  <%= link_to 'Create New Page', new_page_path %>
</p>
<% @pages.each do |page| %> <!-- #will give a link to every page available -->
  <h2><%= page.title %></h2>
  <%= link_to 'Click me!', page_path(page) %>
<% end %>
